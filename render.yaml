services:
  # 1. The Spring Boot Backend (Fixed Runtime)
  - type: web
    name: smartevent-backend
    runtime: docker               # <--- This fixes the "invalid runtime java" error!
    rootDir: backend
    buildCommand: ./mvnw spring-boot:build-image -DskipTests
    startCommand: java -jar target/eventai-0.0.1-SNAPSHOT.jar
    envVars:
      - key: DB_URL
        fromDatabase:
          name: smartevent-db
          property: connectionString
      - key: DB_USERNAME
        fromDatabase:
          name: smartevent-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: smartevent-db
          property: password
      - key: OPENROUTER_API_KEY
        value: "PLACEHOLDER"
      - key: MAIL_USERNAME
        value: "PLACEHOLDER"
      - key: MAIL_PASSWORD
        value: "PLACEHOLDER"

  # 2. The React Frontend
  - type: web
    name: smartevent-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: smartevent-db
    plan: free